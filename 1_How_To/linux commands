ps -eo pid,ppid,cmd,rss,pmem --sort=-rss | head -n 5
ps -eo pid,ppid,cmd,rss,pmem --sort=-rss | head -n 10
free -h
df -h
systemctl list-units --type=service --state=running
iptables -L -n -v --line-numbers
histsory | tail -10
history | tail -10
systemctl stop kubelet
systemctl disable kubelet
ps au | grep kube-
ps aux | egrep 'kube- |etcd'
systemctl stop kubelet
systemctl disable kubelet
ps aux | grep kubelet
systemctl mask kubelet
ps aux | grep kubelet
systemctl unmask kubelet
ps aux | grep kubelet
pkill -9 kube-apiserver
pkill -9 etcd
pkill -9 kube-scheduler
pkill -9 kube-controller-manager
ll /usr/local/bin/
ps aux
ps aux | grep 'kube'
ps aux | grep 'kubelet'
ps aux | grep kube
systemctl stop kubelet
systemctl disable kubelet
systemctl disable containerd
systemctl stop containerd
systemctl stop docker
systemctl disable docker
systemctl stop docker
systemctl stop docker.socket
systemctl disable docker.socket
pkill -9 kube-apiserver
pkill -9 etcd
pkill -9 kube-scheduler
ps aux | grep -E 'kube|etcd|contaierd|docker'
ps -ef | grep kube-apiserver
ps -ef | grep kubelet
ps -ef | grep etcd
pgrep -l kube
pgrep -l etcd
pgrep -l containerd
ctr -n k8s.io containers list | grep -E "3746300|3781692|3784062"
pstree -p 3781692
ps aux | grep containerd-shim | awk '{print $2}' | xargs kill -9
ps -ef | grep containerd-shim | grep -v grep
systemctl stop kubelet
systemctl stop docker
systemctl stop containerd
pstree -p 3781692
pstree -p
killall -9 kube-apiserver etcd kube-scheduler kube-controller-manager kubelet containerd-shim 2>/dev/null
# Kill kube-apiserver
pkill -9 kube-apiserver
# Kill etcd
pkill -9 etcd
# Kill kube-scheduler
pkill -9 kube-scheduler
# Kill kubelet (if still running)
pkill -9 kubelet
# Kill any remaining container processes
pkill -9 containerd
pkill -9 containerd-shim
docker ps
docker stop $(docker ps -aq) 2>/dev/null || true
docker rm $(docker ps -aq) 2>/dev/null || true
ctr -n k8s.io containers rm $(ctr -n k8s.io containers ls -q) 2>/dev/null || true
ctr -n k8s.io tasks kill --all $(ctr -n k8s.io tasks ls -q) 2>/dev/null || true