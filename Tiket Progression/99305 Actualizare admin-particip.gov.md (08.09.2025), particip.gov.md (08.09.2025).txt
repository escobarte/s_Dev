```
1. actualizarea admin-particip.gov.md din GIT. Suplimentar este necesar de aplicat migratiile bazei de date cu comanda php artisan migrate, recomandăm crearea unei copii de rezervă a bazei de date înainte de a rula comanda.
1.1 Snapshot [+]
1.2 actualizarea admin-particip.gov.md din GIT
1.3 dump db
1.4 php artisan migrate
2. actualizarea la particip.gov.md din GIT.
2.1 Snapshot [+]
2.1 actualizarea la particip.gov.md din GIT
Primul oficial SSH connect al meu
1. Teampass -> Proxy -> 
ssh tenantadmin@10.25.15.2
Password: din comentariu <Temp pass>
```
### Commands
```bash
netstat -tlnp | grep postgres
cat .env | grep DB_
grep -i error /var/log/postgresql/postgresql-*-main.log | tail -20
pg_dump -U participadmin -h localhost participadmin > dump_participadmin_$(date +%Y%m%d_%H%M%S).sql
cd /DATA/newparticip.gov.md/
git status
cd htdocs/
git status
git pull
chown -R www-data:www-data .
df -h
cat > ~/tmp.txt
for FILE in $(cat ~/tmp.txt); do ls -l $FILE; done
for FILE in $(cat ~/tmp.txt); do chown www-data $FILE; done
for FILE in $(cat ~/tmp.txt); do ls -l $FILE; done
for FILE in $(cat ~/tmp.txt); do chown www-data: $FILE; done
for FILE in $(cat ~/tmp.txt); do ls -l $FILE; done
cd /DATA/newparticip.gov.md/htdocs/
tail -f /var/log/nginx/admin-particip.gov.md-access.log
проверка что порты базы прослушываются
ls /DATA/
lsof -i:5432
ps axu| grep post
ss -nt" grep 5432

```
### подключаемся к базе
```bash
su - postgres # переключаемся на юзера postgres
psql # заходим в базу
postgres=# 
\l  # list баз данных
\с participadmin # You are now connected to database "participadmin" as user "postgres"
\q
logout

```
### Делаю дамп базы 
```bash
pg_dump -U participadmin -h localhost participadmin > dump_participadmin_$(date +%Y%m%d_%H%M%S).sql
ls -alht
```

### делаем git
```bash
git status # у меня показало что apt to date 
проверям по коммиту
df -h /
df -h .
git fetch origin test
git log --one-line
git status
git pull origin test
chown -R www-data:www:data .
# оказалось что часть была на NFS, выделил фаилы которые были изменены, добавил в файл чтобы потом поменять им права (min 27)
cat > ~/tpm.txt
#Вставляешь текст
for FILE in $(cat ~/tmp.txt); do ls -l $FILE; done
for FILE in $(cat ~/tmp.txt); do chown www-data: $FILE; done
```